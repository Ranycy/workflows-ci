name: MLflow CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          pip install mlflow scikit-learn pandas numpy imbalanced-learn

      - name: Run training
        run: |
          cd MLProject
          python modelling.py

      - name: Build MLflow Docker Image
        run: |
          mlflow models build-docker \
            -m "runs:/c645f51a09284b23b352623066a977d0/random_forest_model" \
            -n endometriosis-model

      - name: Login Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Tag Docker Image
        run: |
          docker tag endometriosis-model ranyyy/endometriosis-model:latest

      - name: Push Docker Image
        run: |
          docker push ranyyy/endometriosis-model:latest
